/* Barra azul con botones azules y hover mÃ¡s oscuro */
.menu {
  position: relative;
  z-index: 1000;
  background: var(--menu-bg);
  color: var(--menu-text);
  box-shadow: var(--menu-shadow);
}

/* Contenedor centrado con el mismo ancho que las secciones */
.menu-inner {
  max-width: 1100px;        /* igual que .band-inner en tratamientos */
  margin: 0 auto;           /* centra el contenido */
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: var(--menu-gap);
  padding: 12px 18px;       /* mantiene el acolchado del menú */
}

.brand {
  display: flex;
  align-items: center;
  gap: 16px;
}

.brand-logo {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: #0d0d0d;
  color: var(--menu-text);
  display: grid;
  place-items: center;
  text-decoration: none;
  font-weight: 700;
  letter-spacing: 0.5px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25) inset,
    0 2px 4px rgba(0, 0, 0, 0.25);
}

.brand-name {
  background: #0b0b0b;
  color: var(--menu-text);
  text-decoration: none;
  padding: 10px 16px;
  border-radius: 4px;
  font-weight: 600;
  white-space: nowrap;
  box-shadow: var(--menu-btn-shadow);
}

.nav-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  align-items: center;
  gap: 28px;
  margin-left: auto; /* empuja los botones a la derecha */
  flex-wrap: wrap;
}

.nav-list a {
  display: block;
  text-decoration: none;
  color: var(--menu-text);
  background: var(--menu-btn);
  padding: 10px 16px;
  border-radius: var(--menu-btn-radius);
  box-shadow: var(--menu-btn-shadow);
  position: relative;
  transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
  font-family: var(--menu-font);
  font-weight: 600;
  letter-spacing: 0.2px;
}

/* Destacar primera palabra con color tipo "FISIO" */
.nav-list a .nav-em {
  color: var(--menu-em);
}

/* Volver a blanco al pasar o activo */
.nav-list a:hover .nav-em,
.nav-list a.active .nav-em {
  color: var(--menu-text);
}
.nav-list a:hover {
  transform: translateY(-1px);
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
  background: var(--menu-btn-active);
}

.nav-list a.active {
  background: var(--menu-btn-active);
}

/* BotÃ³n hamburguesa (oculto en escritorio) */
.menu-toggle {
  border: 0;
  background: transparent;
  color: inherit;
  display: none;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  padding: 8px;
  margin-left: auto; /* en mÃ³vil se sitÃºa a la derecha */
}

.menu-toggle .bar {
  width: 24px;
  height: 2px;
  background: var(--menu-text);
  display: block;
  margin: 5px 0;
  transition: transform 0.2s ease, opacity 0.2s ease;
}

.menu-toggle[aria-expanded="true"] .bar:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}
.menu-toggle[aria-expanded="true"] .bar:nth-child(2) {
  opacity: 0;
}
.menu-toggle[aria-expanded="true"] .bar:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

@media (max-width: 900px) {
  .menu-inner {
    padding: 10px 12px;
  }
  .brand-logo {
    width: 44px;
    height: 44px;
    font-size: 0.95rem;
  }
  .brand-name {
    padding: 8px 12px;
  }
  /* Mostrar hamburguesa y convertir el menÃº en desplegable */
  .menu-toggle {
    display: inline-flex;
  }
  .nav-list {
    display: none;
    position: absolute;
    top: 100%;
    left: 12px;
    right: 12px;
    background: var(--menu-bg);
    padding: 12px;
    margin: 8px 0 0 0;
    border-radius: var(--menu-panel-radius);
    box-shadow: 0 6px 14px rgba(0, 0, 0, 0.25);
    gap: 10px;
    flex-direction: column;
  }
  .nav-list.open {
    display: flex;
  }
  .nav-list a {
    width: 100%;
    text-align: center;
    padding: 12px 16px;
  }
}

/* Overrides: botones del menú del mismo color que el fondo por defecto; cambian solo en hover/selección */
.nav-list a {
  background: var(--menu-bg) !important;
  box-shadow: none !important;
  transform: none !important;
}
.nav-list a:hover,
.nav-list a:focus-visible,
.nav-list a.active {
  background: var(--menu-btn-active) !important;
  box-shadow: none !important;
  transform: none !important;
}

/* Flatten brand name and improve nav-em contrast */
.brand-name {
  background: transparent !important;
  box-shadow: none !important;
}

/* Asegurar legibilidad de la primera palabra sobre fondo igual al botón */
.menu { --menu-em: #1e5aa8; } /* azul tipo logo ajustado */
.nav-list a .nav-em {
  color: var(--menu-em) !important;
}
.nav-list a:hover .nav-em,
.nav-list a.active .nav-em {
  color: var(--menu-text) !important;
  background: transparent;
}



/* Home link: todo el texto en blanco para máxima legibilidad */
.nav-list a.home-link .nav-em { color: var(--menu-text) !important; }
/* Forzar texto blanco en todos los botones del menú (normal/hover/activo) */
.nav-list a, .nav-list a .nav-em { color: var(--menu-text) !important; }
/* Usar imagen del logo en el menú */
.brand-logo {
  width: auto !important;
  height: 56px !important;
  background: transparent !important;
  color: inherit !important;
  box-shadow: none !important;
  border-radius: 0 !important;
  padding: 0 !important;
}
.brand-logo img { display: block; height: 100%; width: auto; }
@media (max-width: 900px) {
  .brand-logo { height: 44px !important; }
}
/* Asegurar barra a ancho completo */
.menu { width: 100%; }


